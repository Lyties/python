services:
  flask:
    entrypoint:
    - sleep
    - infinity
    image: tears999/python:1.0.0
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

